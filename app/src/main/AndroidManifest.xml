<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.sp.senac.appbarti94">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Card_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FAB_Activity">

        </activity>
        <activity android:name=".Principal_Activity" />
        <activity
            android:name=".Splash_Activity"
            android:theme="@style/temaSemBarra"></activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/temaSemBarra" />
    </application>

</manifest>